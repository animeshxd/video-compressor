apt update 
apt upgrade
apt install ffmpeg 
mkdir compressor
cd compressor
wget -r -np -R "index.html*" 
chmod +x crf14
chmod +x crf18
chmod +x crf24
chmod +x crf28
chmod +x crf32
termux-serup-storage
termux-setup-storage
termux-setup-storage
mkdir /sdcard/ffmpeg
